apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "msa-front.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "msa-front.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "msa-front.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "msa-front.name" . }}
    spec:
      containers:
        - name: {{ include "msa-front.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
      imagePullSecrets:
        - name: {{ .Values.image.pullSecretName }}
